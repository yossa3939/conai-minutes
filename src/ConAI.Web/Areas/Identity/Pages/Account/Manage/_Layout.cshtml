@{
    Layout = "/Pages/Shared/_Layout.cshtml";
}

<h1 class="page-title">アカウント管理</h1>

<div class="mt-6 flex flex-col gap-6 md:flex-row">
    <div class="md:w-56 md:shrink-0"><partial name="_ManageNav" /></div>
    <div class="min-w-0 flex-1">@RenderBody()</div>
</div>

@section Scripts {
    @await RenderSectionAsync("Scripts", required: false)
}
