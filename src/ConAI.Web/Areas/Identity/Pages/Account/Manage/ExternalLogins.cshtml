@page
@model ExternalLoginsModel

@{
    ViewData["Title"] = "外部サービスの連携";
    ViewData["ActivePage"] = ManageNavPages.ExternalLogins;
}

<h2 class="section-title">外部サービスの連携</h2>

<partial name="_StatusMessage" for="StatusMessage" />

@if (Model.CurrentLogins?.Count > 0)
{
    <div class="table-scroll mt-4">
        <table class="data-table">
            <thead>
                <tr><th scope="col">サービス</th><th scope="col">操作</th></tr>
            </thead>
            <tbody>
                @foreach (var login in Model.CurrentLogins)
                {
                    <tr>
                        <td id="@($"login-provider-{login.LoginProvider}")">@login.ProviderDisplayName</td>
                        <td>
                            @if (Model.ShowRemoveButton)
                            {
                                <form id="@($"remove-login-{login.LoginProvider}")" asp-page-handler="RemoveLogin" method="post">
                                    <input asp-for="@login.LoginProvider" name="loginProvider" type="hidden" />
                                    <input asp-for="@login.ProviderKey" name="providerKey" type="hidden" />
                                    <button type="submit" class="btn-destructive">連携を解除</button>
                                </form>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}

@if (Model.OtherLogins?.Count > 0)
{
    <div class="card mt-6 max-w-md">
        <h3 class="card-title">連携できるサービス</h3>
        <form id="link-login-form" asp-page-handler="LinkLogin" method="post">
            <div class="mt-4 flex flex-wrap gap-3">
                @foreach (var provider in Model.OtherLogins)
                {
                    <button id="@($"link-login-button-{provider.Name}")" type="submit" class="btn-secondary" name="provider" value="@provider.Name">@provider.DisplayName</button>
                }
            </div>
        </form>
    </div>
}

@if ((Model.CurrentLogins?.Count ?? 0) == 0 && (Model.OtherLogins?.Count ?? 0) == 0)
{
    <div class="alert-note mt-4 max-w-md" role="note">連携できる外部サービスは設定されていません。</div>
}
